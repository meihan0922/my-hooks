// import { add, divide, multiply, subtract } from './math';
import { TestVersion } from '@my-hooks/core';

export interface User {
  name: string;
}

console.log('%c packages/hooks/src/index.ts:8 ', 'color: #007acc;', TestVersion);

// console.log('%c packages/hooks/index.js:1 hooks', 'color: #007acc;');
